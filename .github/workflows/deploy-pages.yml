name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    name: Build & Prepare
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Phase: Checkout
        uses: actions/checkout@v4

      - name: 🟢 Phase: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: 📦 Phase: Install dependencies
        run: |
          echo "::group::Bun Install"
          bun install --frozen-lockfile
          echo "::endgroup::"

      - name: 🏗️ Phase: Build static site
        run: |
          echo "::group::Next.js Build"
          bun run build
          echo "::endgroup::"
        env:
          NEXT_PUBLIC_SITE_URL: https://itamiforge.github.io/itamiforge

      - name: 🛠️ Phase: Post-build cleanup
        run: touch out/.nojekyll

      - name: 📤 Phase: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    name: Deployment
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: 🚀 Phase: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
