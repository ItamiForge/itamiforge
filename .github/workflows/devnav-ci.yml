name: DevNav CI

on:
  push:
    branches: [main]
    paths:
      - 'tools/devnav/**'
      - '.github/workflows/devnav-ci.yml'
  pull_request:
    paths:
      - 'tools/devnav/**'
      - '.github/workflows/devnav-ci.yml'

jobs:
  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: tools/devnav

      - name: Lint check
        working-directory: tools/devnav
        run: |
          echo "::group::Cargo Fmt"
          cargo fmt -- --check
          echo "::endgroup::"

  test:
    name: Test
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: tools/devnav

      - name: Run tests
        working-directory: tools/devnav
        run: |
          echo "::group::Running Tests"
          cargo test --release
          echo "::endgroup::"

  build:
    name: Build
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: tools/devnav

      - name: Build release artifact
        working-directory: tools/devnav
        run: |
          echo "::group::Cargo Build"
          cargo build --release
          echo "::endgroup::"
