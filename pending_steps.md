# Pending steps — itamiforge

No open catalog rollout work.

## Agent / next coding session

1. Keep `catalog/snapshot.json` generated. Do not hand-edit `lib/projects.ts` project lists.
2. Do not ingest private repo file contents onto GitHub Pages.

## Done in this pass

- Hub catalog sync + table, live on GitHub Pages
- Scheduled `catalog-sync` workflow; `GH_PAT` reads private org repos (BrewKogu stub)
- Contract files and README catalog links merged on source repos
- Snapshot refreshed from GitHub contracts
